=================

  Sundae.js a Javascript Canvas Reference Tester
  http://sundae.lighthouseapp.com/dashboard
  http://cwdesautels.blogspot.com/

  The MIT License
  Copyright (c) 2011 Carlin Desautels
  http://www.opensource.org/licenses/mit-license.php

=========================
<script type="text/javascript" src="sundae.js"></script>
<script>
document.addEventListener('DOMContentLoaded', function() {


    var s = Sundae('#divID')
    //
    // Add your tests!
    .addTest({
        blurRadius: 2,//px
        tolerance: 100,//%
        name: "string",
        note: "string",
        tag: "string",
        knownFail: false,
        //Required for the tests!
        firstCanvas :  //"Image Uri" OR "function name" OR function(aCanvas, callback){ definition; callback(); },
        secondCanvas : //"Image Uri" OR "function name" OR function(aCanvas, callback){ definition; callback(); }
    })
    // Add an array of tests
    .addTests([{
        //Test case from above
        },{},{} //More test cases
    ])
    // Add an array of tests from a file
    .addTestFile("jsonFileName")
    //
    // Optional Globals
    .setBlur(2)
    .setTolerance(5)
    .addLibrary("scriptUri")
    .addLibraries([
        "scriptUri",
        "scriptUri"
    ])
    //
    // Optional Events
    .onReady(
        function(){}  
    )
    .onComplete(
        function(){}
    )
    //
    // Can invoke this anytime, Attach it to a button!
    .start();
    //
    // Non Chainable
    s.getTags()
    s.getTests()//Adds pass boolean to test properties
    
}, false);
</script>

=========================================
