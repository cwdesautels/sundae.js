=================

  Sundae.js a Javascript Canvas Reference Tester
  http://sundae.lighthouseapp.com/dashboard
  http://zenit.senecac.on.ca/wiki/index.php/User:Cwdesautels/sundae
  http://cwd89.blogspot.com/
 
  The MIT License
  Copyright (c) 2011 Carlin Desautels
  http://www.opensource.org/licenses/mit-license.php

=========================

  API: resouces/tests.json

=================================

NOTES
* "function(){...}" OR {"src": "url/to/file", "func": "nameOfFunctionInYourFile"}
** When Your function is called it will be passed a Canvas for your library to edit, Note * applies
*** Required field for test to be run, Note ** applies

{

  "globalBlurRadius": int,
  "globalTolerance": float,
  //called once before first suite is started
  "globalSetup": *,
  //called once after last suite is finished
  "globalTeardown": *,
  //called before each suite's setup
  "onGlobalRun": *,
  //called before each suite's teardown
  "onGlobalRunComplete": *,

  //Array or suites within the scope of the program
  "testSuite": [
    { 
      "suiteBlurRadius": int,
      "suiteTolerance": float,
      //called once before suite starts
      "suiteSetup": *,
      //called once after suite ends
      "suiteTeardown": *,
      //called before each test's setup within suite
      "onSuiteRun": *,
      //called after each test's teardown within suite
      "onSuiteRunComplete": *
      //Array of tests within the scope of the suite
      "test": [
        {
          "name": "string",
          "ID": "string",
          "tag": "string",
          "expectedFail": "boolean",
          "expectedPass": "boolean",
          "dependancyURL": ["array of URL strings"],
          "referenceImageURL": "string",
          "messageOnFail": "string",
          "messageOnPass": "string",
          "description": "string",
          "testBlurRadius": int,
          "testTolerance": float,
          "expectedTime": int,
          "run": ***,
          "testSetup": **,
          "testTeardown": **,
          "onTestRun": **,
          "onTestRunComplete": **
        },{//More test objects},{...}
      ]
    },{//More testSuite objects},{...}
  ]
}

=========================================